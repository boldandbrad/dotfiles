require('neo-tree').setup({
    buffers = {
        follow_current_file = {
            enabled = true,
        },
    },
    filesystem = {
        follow_current_file = {
            enabled = true,
        }
    },
    window = {
        position = "right",
        width = 32,
    },
})
