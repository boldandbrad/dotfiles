----------------------------------------------------------------
-- mason.nvim - language server protocol installer            --
----------------------------------------------------------------

return {
  "williamboman/mason.nvim",
  lazy = false,
  opts = {
    ui = { border = "rounded" }
  },
}
