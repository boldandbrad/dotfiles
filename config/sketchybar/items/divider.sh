#!/bin/sh

# initialize
sketchybar --add item divider left

# config
sketchybar --set divider icon.drawing=off
sketchybar --set divider label="|"

# overrides
sketchybar --set divider label.padding_left=0
sketchybar --set divider label.padding_right=8
