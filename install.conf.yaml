- defaults:
    link:
      create: true
      relink: true

- clean: ["~"]

- link:
    ~/.aliases: aliases
    ~/.bashrc: bashrc
    ~/.gitconfig: git/gitconfig
    ~/.gitignore: git/gitignore
    ~/.hyper.js: hyper/hyper.js
    ~/.zshrc: zshrc
